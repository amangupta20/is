# Assistant implementation progress

Baseline: `da14960` on `master`
Working branch: `assistant-foundation`

- Slice 1A: complete (`da14960..98cfa3b`, review clean)
- Slice 1B: complete (`98cfa3b..f262d30`, review clean)
- Housekeeping: scratch reports untracked (`8cea975`)
- Slice 1C: complete (`8cea975..c906cf3`, review clean after Valve persistence fix)
- Slice 1C live pilot: waiting for the Dokploy/Open WebUI deployment target
- Database connectivity: verified through Supavisor as `assistant_core`, current schema `assistant_core`, with USAGE/CREATE
- Slice 1D1: complete (`c906cf3..e843e55`, review clean after readiness/migration-scope hardening)
- Slice 1D2A: complete (`e843e55..4aeddba`, review clean after live-test cleanup hardening)
- Slice 1D2B: complete (`4aeddba..3540e19`, review clean after race-free live-test scoping)
- Slice 1E: complete (`3540e19..e92df69`, review clean after full fail-open hardening)
- Slice 1F1: complete (`e92df69..b261094`, final review clean after telemetry privacy and teardown hardening; `156` full tests passed, `11` expected live-DB skips)
- Slice 1F2A: Dokploy Git/Compose build contract complete (`b261094..c85acd2`, review clean after one-build-owner regression coverage)
- Slice 1F2B: Docker/Oracle ARM64 build, Dokploy rollout, adapter import, and live smoke — pending
- Slice 1F2A: Git/Compose build contract reviewed; focused deployment-assets test and full static suite pass. Docker build, Oracle ARM64 runtime, Dokploy rollout, adapter import, and live smoke remain pending.
- Slice 1F2B live deployment: Git/Compose build and Dokploy rollout succeed on Oracle ARM64; readiness returns 200, lifecycle delivery returns 202, and context lookup returns 200. Live rows show three completed event jobs, but chat/message IDs are absent because the adapter still reads the legacy nested payload shape; correcting the current `subject`/`data` event envelope is the next gate.
- Slice 1F2C: context-filter budget aligned with Open WebUI's 9,999 UI limit (`c85acd2..1485772`, independent review clean; `159` full tests passed, `11` expected live-DB skips).
- Slice 1F2D: current Open WebUI `subject`/`data` lifecycle envelope parsed with legacy fallbacks (`1485772..b555654`, independent review clean; `178` full tests passed, `11` expected live-DB skips). Redeploy and re-import the Event Function, then verify non-null live chat/message IDs.
- Slice 1F2D live verification: newest `chat.finished` and both `message.created` rows contain the expected non-null chat/message IDs; lifecycle envelope compatibility is confirmed against the deployed system.
- Slice 1F2E: the Context Filter uses a text-backed `300000` Valve to bypass Open WebUI's 9,999 numeric-widget ceiling, accepts legacy integer values, and supports the configured temporary `500000` cap (`b555654..5ecb8f3`, review clean after test-strengthening; `198` full tests passed, `11` expected live-DB skips).
- Phase 2A Task 1: durable completed-turn model and worker materialization complete (`4253ad6..288c8e3`, review clean after poisoned-transaction recovery and migration-contract hardening; `234` full tests passed, `12` expected live-DB skips). Live materialization remains gated on deployment of migration `0003_completed_turn`.
- Phase 2A Task 2: fail-open Open WebUI outlet capture complete (`288c8e3..13d7f8c`, independent review clean; exact stable-ID selection, privacy allowlist, deterministic delivery, and the 524,288-byte boundary are covered; `269` full tests passed, `12` expected live-DB skips).
- Phase 2A integration: approved after server-side raw-size enforcement, strict oversized-envelope sanitization, worker lease ownership, and live-fixture repair (`13d7f8c..686afea`; final root verification `312` passed, `13` expected live-DB skips; Ruff, mypy, lock, diff, and committed-secret scans clean). Pushed to `origin/assistant-foundation`; deployment of migration `0003` and re-import/live verification of the Context Filter are the current gate.
- Phase 2B Task 1: complete (`4eff6fc..db51828`, review clean after durable text bounds, exact supersession timestamp/linkage, and stronger assertions in the single representative test).
- Phase 2B Task 2: complete (`db51828..6124dba`, review clean after the focused test proved the committed `memory:<turn_uuid>` extraction-job boundary).
- Phase 2B Task 3: complete (`6124dba..9f10151`, Luna/high review clean; immutable empty/nonempty per-chat profiles and cache-stable Filter placement verified).
- Phase 2B Task 4: complete (`9f10151..134f190`, Luna/high review clean after stripped-query bounds and compact-preview hardening; `17` focused tests passed).
- Phase 2B boundary: complete (`134f190..8dd3ee5`, stale Task 2 deployment-key contract corrected and independently reviewed; `318` passed, `13` skipped; Ruff, mypy, and lock check clean).
- Phase 2B focused fixes: complete (`8dd3ee5..8716a50`; extraction-job finalization, exact provider schema contract, and correction ordering were fixed; the user confirms the current fixes were already locally verified and must not be rerun as a startup ritual).
- Phase 2B live pilot: deployed through Dokploy's automatic CI/CD from pushed `assistant-foundation`. Live Open WebUI evidence confirms a frozen profile loads, lexical `search_personal_context` returns the active record, and `read_personal_context` returns the exact evidence plus native source chat/message IDs. The first genuinely unfinished gate is correction/supersession while proving the current chat profile stays frozen and a new chat receives the correction.
- Phase 2B correction/frozen-profile live gate: complete by user confirmation. Correction/supersession worked, the originating chat retained its frozen profile, and a later new chat received the corrected active preference. Do not repeat this controlled scenario. The first unfinished live gate is the one-time Tool fail-open check, followed by usefulness/cache evaluation.
- Phase 2B Tool fail-open live gate: complete by user confirmation. Reading an invalid memory source returned the safe unavailable result and ordinary chat continued. All controlled Phase 2B correctness gates are complete; usefulness/cache evaluation is now the only gate before choosing the next slice.
- Phase 2B initial usefulness evaluation: accepted with no observed pain reported by the user. Do not begin Phase 2C speculatively. Leave Phase 2B running in normal use and select the next slice only from a concrete limitation, failure, or newly requested capability. Provider cache metrics remain optional evidence if they become available, not a blocker.
- Standing deployment workflow: Dokploy follows the pushed upstream `assistant-foundation` branch automatically. After a push, verify the changed workflow in Open WebUI/Assistant Core rather than asking for routine manual branch/redeploy confirmation. Inspect Dokploy source, migration, service state, or redacted logs only when configuration changed, a migration is expected, CI/CD did not converge, or live behavior fails.
- Phase 2C implementation: complete locally (`8716a50..2573af0`). It adds bounded user/assistant passage indexing, per-user exact-content reuse, PostgreSQL FTS plus pgvector hybrid search, lexical fail-open behavior, bounded neighboring reads, metadata-only worker/retrieval logs, automatic existing-turn backfill, and `chat.deleted` tombstones. Public Open WebUI Tool method names/arguments remain unchanged.
- Phase 2C review/verification: Luna/high found no Critical issues. Worker embedding startup, rollback-safe logging, fixed 1536 dimensions, explicit owner equality, and deleted-evidence filtering were hardened in `2573af0`. Fresh boundary evidence: `333 passed, 13 skipped`; Ruff clean; mypy clean across 45 source files; lock check clean; Alembic offline upgrade through `0005_conversation_recall` clean; committed credential-pattern scan clean. A new local PostgreSQL harness was intentionally deferred because this is an experimental personal system; the live Dokploy migration and recall scenario is the real database gate.
- Phase 2C deployment state: local branch is ahead of `origin/assistant-foundation`; push is the first unfinished gate. Dokploy will deploy automatically after push. New Assistant Core variables required in Dokploy are `ASSISTANT_EMBEDDING_BASE_URL`, `ASSISTANT_EMBEDDING_API_KEY`, `ASSISTANT_EMBEDDING_MODEL`, `ASSISTANT_EMBEDDING_DIMENSION=1536`, and optional timeout. The first manual checkpoint is redacted migration/API/worker state after automatic deployment and embedding-variable configuration; then re-import only `adapters/openwebui/assistant_core_tool.py` and run one controlled cross-chat search/read/delete scenario.
- Phase 2C push: complete through `5b32659`; `origin/assistant-foundation` advanced from `8716a50` and Dokploy automatic deployment was triggered. The user's unrelated `.gitignore` remains the only local modification. First unfinished gate: save the five `ASSISTANT_EMBEDDING_*` Dokploy names (values remain private), then interpret automatic/restarted migration `0005`, API/worker health, and metadata-only backfill/index logs.
- Phase 2C live hardening (Linux migration): `assistant-core/.venv` Windows → Linux rebuilt (`uv` 3.12.11, `chmod 644` fix for `EXE002`, `pgvector` ignore removed) — `333 passed, 13 skipped`, `ruff/mypy` clean. `deploy/postgres/bootstrap_assistant_role.sql` now `GRANT USAGE ON SCHEMA extensions` + `search_path=assistant_core,extensions,public`; `0005` now `SET search_path` + `CREATE EXTENSION IF NOT EXISTS vector SCHEMA extensions`. Persistent `pg_db_role_setting` verified as `search_path=assistant_core, extensions, public`. Migration `0005` live-verified (`alembic_version=0005_conversation_recall`, both `conversation_*` tables).
- Phase 2C empty-env hardening: `assistant_core/config.py` now coerces `''` → `None` for `ASSISTANT_EMBEDDING_*`/`TASK_MODEL_*`/`OTLP` — lexical fallback instead of `ValidationError: string_too_short` crash (`3b35fe7`, `ruff/mypy` clean, `333 passed`).
- Phase 2C live: `vertex_ai/gemini-embedding-2` hybrid proven live (`conversation_index_completed dimension=1536 embedded_count` for `31eaaf9c`, `ad5f7c31`), `show_index_stats`/`show_recent_conversations` later added. Verbose `preference.technical_help_format (31710661…)` archived via direct `assistant_core` DB (Supabase `SET ROLE` blocked), concise `ad5f7c31…` active — new chats now freeze 1-source concise, `tombstoned_file_references` logic verified via `live_memory_evidence_clause`.
- Inspection (Phase 2D lean): signed `POST /v1/inspection/recent` + `/stats` (metadata-only, no content), repository `get_recent_references`/`get_conversation_stats`, Tool `show_recent_conversations`/`show_index_stats` (fail open to `_UNAVAILABLE`). No `pytest` ceremony per user — `ruff/mypy` clean, live verified: `Recent 5` + `Index stats: segments 50 (19 embedded) refs 60 (44 active,16 tombstoned) jobs 0/21` (`5baaecb`).
- File awareness Task 1: `file_segment`/`file_reference` (`file-v1`, 4k/400, `SHA-256` per-user dedup, `search_vector` GIN + `VECTOR(1536)` HNSW), migration `0006_file_awareness` (`assistant_core.files` registered), `materialize_file` + `get_file_stats`/`search_file_context` (FTS + pgvector RRF, fail open), `search_personal_context` now merges `memory|conversation|file` (`4eeb0cd`), `show_index_stats` now also returns `total_file_*` counters.
- File awareness hook: `POST /v1/files/materialize` (signed, `500k` cap, `hash → chunk → embed` with `get_conversation_embedder`, `embedded_at`), `file_index_filter.py` inlet (Library + paperclip, HMAC, 500k cap, fail-open), `POST /v1/files/tombstone` + `tombstone_file` repository, worker `file.deleted` → `tombstone_file` via `lifecycle_event` (`file.deleted` allowlisted), `file_reference` tombstones (`f389154`). Live `file_materialize_completed` pending user Library upload.
- File deleted semantics: `file_reference` is per `native_file_id` (not `chat_id`) — deleting a chat tombstones only `conversation_reference`/`completed_turn`; Library files stay `active` until `file.deleted` or memory-manager archive. `file.deleted` via WebUI → `lifecycle_event` → worker, or via direct `POST /v1/files/tombstone` from memory-manager UI.
- Current HEAD: `f389154` on `assistant-foundation`, in sync with `origin/assistant-foundation`. Only `.gitignore` remains unstaged (user-owned). Linux Arch workstation, `uv` 3.12.11, `50` source files, `ruff/mypy` clean. Next session continues from file-delete verification or `4 → memory manager` (interactive archive/merge via Tool) per user order `1→2→4→3`; `3 → history import` is last. Handoff is `.superpowers/sdd/handoff-2026-08-12.md`.

The sequence is intentionally split smaller than the original Phase 0–1 plan. Each slice is tested and committed independently. Docker/ARM work is deferred until a deployable slice because Docker is unavailable on the current workstation.
